[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "refactor-method-tool"
version = "0.1.0"
description = "Extract Method refactoring tool for Python using ast"
requires-python = ">=3.10"
